name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Extract .NET version from csproj
      id: dotnet-version
      run: |
        # Extract TargetFramework from csproj (e.g., net8.0 -> 8)
        DOTNET_VERSION=$(sed -n 's/.*<TargetFramework>net\([0-9]\+\).*/\1/p' src/BulkUpload/BulkUpload.csproj)
        echo "version=$DOTNET_VERSION" >> $GITHUB_OUTPUT
        echo "Detected .NET version: $DOTNET_VERSION"

        # Set flag for npm usage (>= 10 requires npm)
        if [ "$DOTNET_VERSION" -ge 10 ]; then
          echo "use_npm=true" >> $GITHUB_OUTPUT
          echo "Will use npm for .NET $DOTNET_VERSION"
        else
          echo "use_npm=false" >> $GITHUB_OUTPUT
          echo "Will NOT use npm for .NET $DOTNET_VERSION"
        fi

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ steps.dotnet-version.outputs.version }}.0.x

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json', '**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Restore dependencies
      run: dotnet restore src/BulkUpload.sln --disable-parallel

    # Setup Node.js for BulkUpload.V17 frontend
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'

    - name: Cache npm packages
      uses: actions/cache@v4
      with:
        path: src/BulkUpload.V17/Client/node_modules
        key: ${{ runner.os }}-npm-${{ hashFiles('src/BulkUpload.V17/Client/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-npm-

    - name: Install V17 frontend dependencies
      working-directory: src/BulkUpload.V17/Client
      run: npm ci

    - name: Build V17 frontend
      working-directory: src/BulkUpload.V17/Client
      run: npm run build

    - name: Build and test solution
      shell: bash
      run: |
        # Initialize summary file
        echo "## 🔨 Build and Test Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        sln="src/BulkUpload.sln"
        sln_name=$(basename "$sln")

        echo "### 📁 $sln_name" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        echo "Building solution (skipping dotnet format)..."
        dotnet build "$sln" --configuration Release --no-restore -p:SkipPreBuild=true 2>&1 | tee build.log
        build_exit=${PIPESTATUS[0]}

        # Parse build output for errors and warnings
        errors=$(grep -c "error [A-Z]*[0-9]*:" build.log || true)
        warnings=$(grep -c "warning [A-Z]*[0-9]*:" build.log || true)

        if [ $build_exit -eq 0 ]; then
          echo "✅ **Build:** Succeeded" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Build:** Failed" >> $GITHUB_STEP_SUMMARY
        fi
        echo "- Errors: $errors" >> $GITHUB_STEP_SUMMARY
        echo "- Warnings: $warnings" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # Show first 10 errors if any
        if [ $errors -gt 0 ]; then
          echo "<details><summary>🔴 Build Errors</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          grep "error [A-Z]*[0-9]*:" build.log | head -10 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi

        # Show first 10 warnings if any
        if [ $warnings -gt 0 ]; then
          echo "<details><summary>⚠️ Build Warnings</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          grep "warning [A-Z]*[0-9]*:" build.log | head -10 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi

        # Only run tests if build succeeded
        if [ $build_exit -eq 0 ]; then
          echo "Running tests..."
          dotnet test "$sln" --configuration Release --no-build --verbosity normal 2>&1 | tee test.log
          test_exit=${PIPESTATUS[0]}

          # Parse test results
          total_tests=$(grep -oP 'Total tests: \K\d+' test.log || echo "0")
          passed_tests=$(grep -oP 'Passed: \K\d+' test.log || echo "0")
          failed_tests=$(grep -oP 'Failed: \K\d+' test.log || echo "0")
          skipped_tests=$(grep -oP 'Skipped: \K\d+' test.log || echo "0")
          test_time=$(grep -oP 'Total time: \K[\d.]+' test.log || echo "0")

          if [ $test_exit -eq 0 ]; then
            echo "✅ **Tests:** Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Tests:** Failed" >> $GITHUB_STEP_SUMMARY
          fi

          # Add test details
          if [ "$total_tests" != "0" ]; then
            echo "- Total: $total_tests" >> $GITHUB_STEP_SUMMARY
            echo "- Passed: $passed_tests ✅" >> $GITHUB_STEP_SUMMARY
            if [ "$failed_tests" != "0" ]; then
              echo "- Failed: $failed_tests ❌" >> $GITHUB_STEP_SUMMARY
            fi
            if [ "$skipped_tests" != "0" ]; then
              echo "- Skipped: $skipped_tests ⏭️" >> $GITHUB_STEP_SUMMARY
            fi
            echo "- Duration: ${test_time}s ⏱️" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          # Show test failures if any
          if [ $test_exit -ne 0 ]; then
            echo "<details><summary>❌ Test Failures</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            grep -A 5 "Failed!" test.log | head -20 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          # Exit with test exit code
          exit $test_exit
        else
          echo "⏭️ **Tests:** Skipped (build failed)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi

        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # Add overall summary
        echo "## 📊 Overall Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Total Errors:** $errors" >> $GITHUB_STEP_SUMMARY
        echo "- **Total Warnings:** $warnings" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

    - name: Verify package contents and v17 RCL assets
      if: success()
      shell: bash
      run: |
        echo "## 📦 Package Verification" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # Find all NuGet packages
        packages=$(find . -name "*.nupkg" -not -name "*.symbols.nupkg" 2>/dev/null || true)

        if [ -z "$packages" ]; then
          echo "⚠️ No packages found to verify" >> $GITHUB_STEP_SUMMARY
          exit 0
        fi

        # Create temp directory for extraction
        temp_dir=$(mktemp -d)

        for pkg in $packages; do
          pkg_name=$(basename "$pkg")
          echo "### 📦 $pkg_name" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Extract package
          extract_dir="$temp_dir/${pkg_name%.nupkg}"
          mkdir -p "$extract_dir"
          unzip -q "$pkg" -d "$extract_dir" 2>/dev/null || {
            echo "❌ Failed to extract package" >> $GITHUB_STEP_SUMMARY
            continue
          }

          # Check for v17 package (contains V17 in name or targets net10.0)
          is_v17=false
          if [[ "$pkg_name" == *"V17"* ]] || [ -d "$extract_dir/lib/net10.0" ]; then
            is_v17=true
          fi

          if [ "$is_v17" = true ]; then
            echo "🔍 **Verifying v17 RCL assets...**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Check for App_Plugins/BulkUpload/dist directory
            dist_found=false
            if [ -d "$extract_dir/content/App_Plugins/BulkUpload/dist" ] || \
               [ -d "$extract_dir/contentFiles/any/any/App_Plugins/BulkUpload/dist" ] || \
               [ -d "$extract_dir/App_Plugins/BulkUpload/dist" ]; then
              dist_found=true
            fi

            if [ "$dist_found" = true ]; then
              echo "✅ v17 dist folder found" >> $GITHUB_STEP_SUMMARY

              # List key files in dist
              dist_files=$(find "$extract_dir" -path "*/App_Plugins/BulkUpload/dist/*" -type f 2>/dev/null | wc -l)
              echo "- Found $dist_files built assets in dist folder" >> $GITHUB_STEP_SUMMARY

              # Check for specific expected files
              js_files=$(find "$extract_dir" -path "*/App_Plugins/BulkUpload/dist/*.js" -type f 2>/dev/null | wc -l)
              if [ "$js_files" -gt 0 ]; then
                echo "- ✅ JavaScript bundles found ($js_files files)" >> $GITHUB_STEP_SUMMARY
              else
                echo "- ⚠️ No JavaScript bundles found" >> $GITHUB_STEP_SUMMARY
              fi

              # Check for umbraco-package.json or manifest
              if find "$extract_dir" -name "umbraco-package.json" -o -name "manifest.json" 2>/dev/null | grep -q .; then
                echo "- ✅ Extension manifest found" >> $GITHUB_STEP_SUMMARY
              else
                echo "- ⚠️ Extension manifest not found" >> $GITHUB_STEP_SUMMARY
              fi
            else
              echo "❌ v17 dist folder NOT found" >> $GITHUB_STEP_SUMMARY
              echo "⚠️ Expected: App_Plugins/BulkUpload/dist/" >> $GITHUB_STEP_SUMMARY
            fi

            # Check DLL for embedded resources (StaticWebAssets)
            dll_path=$(find "$extract_dir" -name "BulkUpload*.dll" -not -path "*/ref/*" | head -1)
            if [ -n "$dll_path" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "🔍 **Checking DLL for embedded resources...**" >> $GITHUB_STEP_SUMMARY

              # Use strings command to check for embedded resource markers
              if strings "$dll_path" 2>/dev/null | grep -q "Microsoft.AspNetCore.StaticWebAssets"; then
                echo "- ✅ StaticWebAssets metadata found in DLL" >> $GITHUB_STEP_SUMMARY
              else
                echo "- ℹ️ No StaticWebAssets metadata detected (may use content files instead)" >> $GITHUB_STEP_SUMMARY
              fi
            fi
          else
            # v13 package verification
            echo "🔍 **Verifying v13 package assets...**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Check for App_Plugins directory
            app_plugins_found=false
            if find "$extract_dir" -type d -path "*/App_Plugins/BulkUpload" 2>/dev/null | grep -q .; then
              app_plugins_found=true
            fi

            if [ "$app_plugins_found" = true ]; then
              echo "✅ App_Plugins/BulkUpload folder found" >> $GITHUB_STEP_SUMMARY

              # Count files
              total_files=$(find "$extract_dir" -path "*/App_Plugins/BulkUpload/*" -type f 2>/dev/null | wc -l)
              js_files=$(find "$extract_dir" -path "*/App_Plugins/BulkUpload/*.js" -type f 2>/dev/null | wc -l)
              html_files=$(find "$extract_dir" -path "*/App_Plugins/BulkUpload/*.html" -type f 2>/dev/null | wc -l)

              echo "- Total assets: $total_files files" >> $GITHUB_STEP_SUMMARY
              echo "- JavaScript files: $js_files" >> $GITHUB_STEP_SUMMARY
              echo "- HTML files: $html_files" >> $GITHUB_STEP_SUMMARY

              # Check for buildTransitive targets
              if [ -f "$extract_dir/buildTransitive/Umbraco.Community.BulkUpload.targets" ]; then
                echo "- ✅ MSBuild targets found (buildTransitive)" >> $GITHUB_STEP_SUMMARY
              else
                echo "- ⚠️ MSBuild targets not found" >> $GITHUB_STEP_SUMMARY
              fi
            else
              echo "❌ App_Plugins/BulkUpload folder NOT found" >> $GITHUB_STEP_SUMMARY
            fi
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
        done

        # Cleanup
        rm -rf "$temp_dir"

        echo "✅ Package verification complete" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
